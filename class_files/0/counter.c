#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int counter = 0; //initialize counter
    counter = counter + 1; //increment counter
    counter += 1; //also increment
    counter++; //aaaaand again
}